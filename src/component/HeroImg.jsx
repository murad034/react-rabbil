const HeroImg = (props) => {
    return (
        <div>
            <h1>{props.title} from heroimg section</h1>
            <h4>{props.description} from hero img section</h4>
        </div>
    )
}

export default HeroImg;